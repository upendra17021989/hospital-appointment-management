import React from 'react';

const ConsultationReceipts = () => {
  return (
    <div>
      <div className="page-header">
        <div>
          <h1 className="page-title">Consultation Receipts</h1>
          <p className="page-subtitle">Receipts dashboard (to be completed)</p>
        </div>
      </div>
      <div className="card" style={{ padding: 16 }}>
        <p style={{ color: 'var(--text-muted)' }}>
          This page will be expanded to include summary cards, search/filter, paginated/sorted table, and actions.
        </p>
      </div>
    </div>
  );
};

export default ConsultationReceipts;

